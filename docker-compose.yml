  services:
    mylocal_ubuntu22: 
      image: ubuntu:22.10
      container_name: mylocal_ubuntu22
      stdin_open: true # docker run -i
      tty: true # docker run -t
      volumes:
        - $PWD/user_data:/opt/user_data
      # network_mode: host
